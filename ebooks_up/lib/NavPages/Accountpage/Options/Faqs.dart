import 'package:flutter/material.dart';
class Faqs extends StatelessWidget {
  static const String id='Faqs';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('FAQ\'s & Support')),
      ),
    );
  }
}

