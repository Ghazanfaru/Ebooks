import 'package:flutter/material.dart';
class Shortcutss extends StatelessWidget {
  static const String id='Shortcutss';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Shortcuts')),
      ),
    );
  }
}
